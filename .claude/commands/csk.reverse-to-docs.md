---
description: Perform to organize the updated detail to update the master documents in docs folder.
---

# Steps
## 1 Collect the main points to update to docs folder
- Look through the spec documents in the specs/${current branch name} to summarize the main points to update to master documents.
- Also check the plan.md file to know which file to check for the implementation detail.

## 2 Confirm the update content summary with user
- check `docs/README.md` to know which file to update with which contents.
- Create a summary of what to update to which file, and confirm the content with user.
- If user asks for modification, make that modification and re-confirm that.
- If user confirms, proceed to next step

## 3 Make update to the master documents
- Based on the summary made in previous step, make update to the master documents